{
  "name": "project",
  "version": "1.0.0",
  "description": "socialMediaApp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev" : "  concurrently  \"   cd client && npm start            \"          \" cd server && npm run dev \"  " 

  },
  "author": "Sabrine ben sassi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
